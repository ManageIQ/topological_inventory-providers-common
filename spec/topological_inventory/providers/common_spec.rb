RSpec.describe TopologicalInventory::Providers::Common do
  it "has a version number" do
    expect(TopologicalInventory::Providers::Common::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
